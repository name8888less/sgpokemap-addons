/*UL mandibuzz */
var mandibuzzUL = [
    {iv: "15/15/15", lvl: 51},
    {iv: "15/14/15", lvl: 51},
    {iv: "15/15/14", lvl: 51},
];

var mandibuzzIvsUL = mandibuzzUL.map(function(item){
    return item.iv
});

var mandibuzzLvlsUL = mandibuzzUL.map(function(item){
    return item.lvl
});


/*GL mandibuzz */
var mandibuzzGL = [
    {iv: "0/13/15", lvl: 27.5},
    {iv: "0/14/14", lvl: 27.5},
    {iv: "0/15/13", lvl: 27.5},
    {iv: "0/12/15", lvl: 27.5},
    {iv: "2/15/15", lvl: 27},
    {iv: "0/13/14", lvl: 27.5},
    {iv: "1/10/15", lvl: 27.5},
    {iv: "0/14/13", lvl: 27.5},
    {iv: "0/10/10", lvl: 28},
    {iv: "1/11/14", lvl: 27.5},
    {iv: "0/15/12", lvl: 27.5},
    {iv: "0/15/11", lvl: 27.5},
    {iv: "0/11/9", lvl: 28},
    {iv: "1/12/13", lvl: 27.5},
    {iv: "0/12/7", lvl: 28},
    {iv: "1/13/11", lvl: 27.5},
    {iv: "0/11/15", lvl: 27.5},
    {iv: "2/14/15", lvl: 27},
    {iv: "0/7/13", lvl: 28},
    {iv: "0/13/6", lvl: 28},
    {iv: "0/12/14", lvl: 27.5},
    {iv: "1/14/10", lvl: 27.5},
    {iv: "2/15/14", lvl: 27},
    {iv: "2/15/13", lvl: 27},
    {iv: "0/8/12", lvl: 28},
    {iv: "1/9/15", lvl: 27.5},
    {iv: "0/13/13", lvl: 27.5},
    {iv: "0/14/5", lvl: 28},
    {iv: "1/15/9", lvl: 27.5},
    {iv: "1/15/8", lvl: 27.5},
];

var mandibuzzIvsGL = mandibuzzGL.map(function(item){
    return item.iv
});

var mandibuzzLvlsGL = mandibuzzGL.map(function(item){
    return item.lvl
});

/*LC mandibuzz */
var mandibuzzLC = [
    {iv: "3/15/14", lvl: 9},
    {iv: "0/3/11", lvl: 9.5},
    {iv: "4/15/11", lvl: 9},
    {iv: "0/1/14", lvl: 9.5},
    {iv: "0/5/9", lvl: 9.5},
    {iv: "0/9/4", lvl: 9.5},
    {iv: "0/11/2", lvl: 9.5},
    {iv: "3/14/15", lvl: 9},
    {iv: "3/14/14", lvl: 9},
    {iv: "0/13/0", lvl: 9.5},
    {iv: "4/12/14", lvl: 9},
    {iv: "4/14/12", lvl: 9},
    {iv: "4/14/11", lvl: 9},
    {iv: "0/2/12", lvl: 9.5},
    {iv: "0/2/11", lvl: 9.5},
    {iv: "0/4/10", lvl: 9.5},
    {iv: "0/4/9", lvl: 9.5},
    {iv: "0/0/15", lvl: 9.5},
    {iv: "0/0/14", lvl: 9.5},
    {iv: "0/6/8", lvl: 9.5},
    {iv: "0/6/7", lvl: 9.5},
    {iv: "2/15/15", lvl: 9},
    {iv: "2/15/14", lvl: 9},
    {iv: "0/8/5", lvl: 9.5},
    {iv: "0/8/4", lvl: 9.5},
    {iv: "0/10/3", lvl: 9.5},
    {iv: "0/10/2", lvl: 9.5},
    {iv: "1/0/11", lvl: 9.5},
    {iv: "3/13/15", lvl: 9},
    {iv: "3/13/14", lvl: 9},
];

var mandibuzzIvsLC = mandibuzzLC.map(function(item){
    return item.iv
});

var mandibuzzLvlsLC = mandibuzzLC.map(function(item){
    return item.lvl
});

/*LC vullaby */
var vullabyLC = [
    {iv: "3/15/14", lvl: 9},
    {iv: "0/3/11", lvl: 9.5},
    {iv: "4/15/11", lvl: 9},
    {iv: "0/1/14", lvl: 9.5},
    {iv: "0/5/9", lvl: 9.5},
    {iv: "0/9/4", lvl: 9.5},
    {iv: "0/11/2", lvl: 9.5},
    {iv: "3/14/15", lvl: 9},
    {iv: "3/14/14", lvl: 9},
    {iv: "0/13/0", lvl: 9.5},
    {iv: "4/12/14", lvl: 9},
    {iv: "4/14/12", lvl: 9},
    {iv: "4/14/11", lvl: 9},
    {iv: "0/2/12", lvl: 9.5},
    {iv: "0/2/11", lvl: 9.5},
    {iv: "0/4/10", lvl: 9.5},
    {iv: "0/4/9", lvl: 9.5},
    {iv: "0/0/15", lvl: 9.5},
    {iv: "0/0/14", lvl: 9.5},
    {iv: "0/6/8", lvl: 9.5},
    {iv: "0/6/7", lvl: 9.5},
    {iv: "2/15/15", lvl: 9},
    {iv: "2/15/14", lvl: 9},
    {iv: "0/8/5", lvl: 9.5},
    {iv: "0/8/4", lvl: 9.5},
    {iv: "0/10/3", lvl: 9.5},
    {iv: "0/10/2", lvl: 9.5},
    {iv: "1/0/11", lvl: 9.5},
    {iv: "3/13/15", lvl: 9},
    {iv: "3/13/14", lvl: 9},
];

var vullabyIvsLC = vullabyLC.map(function(item){
    return item.iv
});

var vullabyLvlsLC = vullabyLC.map(function(item){
    return item.lvl
});

function refreshPokemons() {
    if (!shouldUpdate) {
        return; //don't update when map is moving
    }
    var toBeRemovedIndexes = [];
    var currentUnixTime = Math.floor(Date.now() / 1000) - timeOffset;
    for (var i = 0; i < pokemons.length; ++i) {
        var currentPokemon = pokemons[i];
        if (currentPokemon.despawn < currentUnixTime - 10 ||
            (!isPokemonChecked(currentPokemon.id) && !shouldTurnFilterOff())) {
            toBeRemovedIndexes.push(i);
        }
    }

    for (var i = toBeRemovedIndexes.length - 1; i >= 0; --i) {
        pokemons.splice(toBeRemovedIndexes[i], 1);
        var marker = markers[toBeRemovedIndexes[i]];
        marker.removeFrom(map);
        markers.splice(toBeRemovedIndexes[i], 1);
    }

    //remove low IV from map, add high IV to map

    for (var i = 0; i < pokemons.length; ++i) {
        var currentPokemon = pokemons[i];

        // fix to adjust for -1 attack, probably screen capture error
        var hasValidStats = false;
        if (currentPokemon.attack>= 1 || currentPokemon.defence >= 1 || currentPokemon.stamina >= 1 ) {
            hasValidStats = true;
            if (currentPokemon.attack== -1) {
                currentPokemon.attack = 0;
            }
            if (currentPokemon.defence== -1) {
                currentPokemon.defence = 0;
            }
            if (currentPokemon.stamina== -1) {
                currentPokemon.stamina = 0;
            }
        }

        var ivPercentage = (currentPokemon.attack + currentPokemon.defence + currentPokemon.stamina) / 45 * 100;
        var marker = markers[i];

        var min_iv_compare = min_iv;

        //to let unknown iv show
        if (min_iv == 0) {
            min_iv_compare = -100;
        }

        if ((ivPercentage >= min_iv_compare && hasValidStats) || shouldTurnFilterOff()) {
            var isDesiredPokemon = false;
            var statsString = currentPokemon.attack+ "/"+ currentPokemon.defence +"/"+currentPokemon.stamina;

            if (currentPokemon.id==629) {

                var index = mandibuzzIvsGL.indexOf(statsString);
                var pvpString = "";
                if (index != -1) {
                    //found. check level
                    targetPokemonLevel = mandibuzzLvlsGL[index];
                    if (currentPokemon.level < targetPokemonLevel)
                    {
                        isDesiredPokemon = true;
                        pvpString = "GL #" + (index+1);
                    }
                }

                index = mandibuzzIvsUL.indexOf(statsString);
                if (index!= -1) {
                    targetPokemonLevel = mandibuzzLvlsUL[index];
                    if (currentPokemon.level < targetPokemonLevel)
                    {
                        //found
                        if (isDesiredPokemon) {
                            pvpString += ", "
                        }
                        isDesiredPokemon = true;
                        pvpString += "UL #" + (index+1);
                    }
                }

                index = mandibuzzIvsLC.indexOf(statsString);
                if (index!= -1) {
                    targetPokemonLevel = mandibuzzLvlsLC[index];
                    if (currentPokemon.level < targetPokemonLevel)
                    {
                        //found
                        if (isDesiredPokemon) {
                            pvpString += ", "
                        }
                        isDesiredPokemon = true;
                        pvpString += "LC Mandi#" + (index+1);
                    }
                }

                index = vullabyIvsLC.indexOf(statsString);
                if (index!= -1) {
                    targetPokemonLevel = vullabyLvlsLC[index];
                    if (currentPokemon.level < targetPokemonLevel)
                    {
                        //found
                        if (isDesiredPokemon) {
                            pvpString += ", "
                        }
                        isDesiredPokemon = true;
                        pvpString += "LC Vullaby#" + (index+1);
                    }
                }


                currentPokemon.pvptype = pvpString;

            }

            if (isDesiredPokemon) {
                if (!marker._map) {
                    marker.addTo(map);
                }
            }
            else {
                if (marker._map) {
                    marker.removeFrom(map);
                }
            }
        }
        else {
            if (marker._map) {
                marker.removeFrom(map);
            }
        }
    }

    if (shouldShowTimers()) {
        for (var i = 0; i < markers.length; ++i) {
            //only update for the ones in bounds
            var mapBounds = map.getBounds();
            var tmpMarker = markers[i];
            if (mapBounds.contains(tmpMarker.getLatLng())) {
                $(tmpMarker._icon).find('.pokemon_icon_timer').html(timeToString(pokemons[i].remainingTime()));
            }
        }
    }
}

function infoWindowString(pokemon) {

    var disguiseString = "";
    if (pokemon.disguise != 0 && pokemon.disguise != -1) {
        disguiseString = " (" + getDisguisePokemonName(pokemon) + ")";
    }

    var genderString = getGenderString(pokemon);

    var formString = getFormString(pokemon);

    var ivString = "";

    var movesetString = "";

    var cpString = "";
    var weatherString = "";

    weatherString = "<a href='faq.html#weather'><b>Weather boost</b>: ";
    switch (pokemon.weather) {
        case 0:
            weatherString += "None<br /></a>";
            break;
        case 1:
            weatherString += "Clear<br /></a>";
            break;
        case 2:
            weatherString += "Rainy<br /></a>";
            break;
        case 3:
            weatherString += "Partly Cloudy<br /></a>";
            break;
        case 4:
            weatherString += "Cloudy<br /></a>";
            break;
        case 5:
            weatherString += "Windy<br /></a>";
            break;
        case 6:
            weatherString += "Snow<br /></a>";
            break;
        case 7:
            weatherString += "Fog<br /></a>";
            break;
        default:
            weatherString = "";
            break;
    }

    // fix to adjust for -1 attack, probably screen capture error
    if (pokemon.attack>= 1 || pokemon.defence >= 1 || pokemon.stamina >= 1 ) {
        if (pokemon.attack== -1) {
            pokemon.attack = 0;
        }
        if (pokemon.defence== -1) {
            pokemon.defence = 0;
        }
        if (pokemon.stamina== -1) {
            pokemon.stamina = 0;
        }
    }

    if (pokemon.attack != -1 && pokemon.defence != -1 && pokemon.stamina != -1 && pokemon.move1 != -1 && pokemon.move2 != -1 && pokemon.cp != -1) {
        ivString = "<b>L30+ IV:</b> "+ pokemon.attack + " | " + pokemon.defence + " | " + pokemon.stamina + " (" + Math.floor((pokemon.attack + pokemon.defence + pokemon.stamina)/45 * 100) + "%)<br />";
        movesetString = "<b>L30+ Moveset:</b><br />" + getMoveName(pokemon.move1) + " | " + getMoveName(pokemon.move2) + "<br />";
        cpString = "<b>L30+ CP:</b> " + pokemon.cp + " (Level: " + pokemon.level + ")<br /><br />";
    }

    return '<b>' + getPokemonName(pokemon) + disguiseString + genderString + formString + "</b><br />" + (pokemon.pvptype ?   pokemon.pvptype + "</b><br /><br />" : "") + weatherString + ivString + movesetString + cpString + timeToString(pokemon.remainingTime()) + '<br /><br /><a target="_blank" href="https://maps.google.com/maps?q=' + pokemon.center.lat + ',' + pokemon.center.lng + '">Maps</a><br /><br /><a href="faq.html#approximately_timer" target="_blank">(Approximately Despawn Time)</a>';
}
